<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="db"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="testing"/>
        <env name="DB_USERNAME" value="db"/>
        <env name="DB_PASSWORD" value="db"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>

        <!--
            The second belt, behind Http::preventStrayRequests() in TestCase.
            That one stops the request; these make sure that if anything ever
            gets past it, it does not travel with a real credential. A test
            that needs a key sets one with config() in its own setUp.
        -->
        <env name="GEMINI_API_KEY" value=""/>
        <env name="PIXABAY_API_KEY" value=""/>
        <env name="UNSPLASH_ACCESS_KEY" value=""/>

        <!-- Never the production bucket, and never the host's ffmpeg. -->
        <env name="MEDIA_DISK" value="public"/>
        <env name="FLASHAI_COMPRESS_AUDIO" value="false"/>
    </php>
</phpunit>
